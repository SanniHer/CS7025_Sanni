function displayScore(inputScore) {

    console.log(inputScore);
    
}

function addScore(newScore) {

    return newScore + 1;

}

currentScore = addScore(20);

displayScore(currentScore)

currentScore = addScore(currentScore)
currentScore = addScore(currentScore)
currentScore = addScore(currentScore)
currentScore = addScore(currentScore)
currentScore = addScore(currentScore)

currentScore = addScore(currentScore)
currentScore = addScore(currentScore)

displayScore(currentScore)